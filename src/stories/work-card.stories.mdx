import moment from 'moment';
import { Meta, Story, Preview, Code, Props } from '@storybook/addon-docs/blocks';
import WorkCard from '../components/layouts/work-card';

<Meta title="Atoms|Work-card" component={WorkCard} />

# WorkCard

This section describe the WorkCard component.

<div style={{ display: 'flex', justifyContent: 'space-between', width: '600px' }}>
    <WorkCard title="My fav Song" date="2020-04-20" description="smoke weed every day" />
</div>

Now let's see in details !

```javascript
<WorkCard src={image} alt={imageAlt} title={title} date={moment(date).formated('DD-MM-YYYY')} description={description} coverimg={coverimg} />
```
# Props

<Props of={WorkCard} />
