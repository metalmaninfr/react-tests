import { Meta, Story, Preview, Code, Props } from '@storybook/addon-docs/blocks';
import { Small, Medium, Large} from './label.stories';
import Label from '../components/atoms/label';

<Meta title="Atoms|Label" component={Label} />

# Label

This section describe all variant of Label component. You can play with 3 sizes: `small`, `medium` and `large`.

<div style={{ display: 'flex', justifyContent: 'space-between', width: '600px' }}>
    <Small />
    <Medium />
    <Large />
</div>

Now let's see in details !

```javascript
<Label size="large">Large Label</Label>
```

# Props

<Props of={Label} />