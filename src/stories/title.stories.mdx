import { Meta, Story, Preview, Code, Props } from '@storybook/addon-docs/blocks';
import { Small, Medium, Large} from './title.stories';
import Title from '../components/atoms/title';

<Meta title="Atoms|Title" component={Title} />

# Title

This section describe all variant of Title component. You can play with 3 sizes: `small`, `medium` and `large`.

<div style={{ display: 'flex', justifyContent: 'space-between', width: '600px' }}>
    <Small />
    <Medium />
    <Large />
</div>

Now let's see in details !

```javascript
<Title size="large">Large title</Title>
```

# Props

<Props of={Title} />
